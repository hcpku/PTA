#include<stdio.h>

int main() {
    char c;
    int i = 1;
    for (; scanf;) {
        scanf("%c", c);
        if (c == ' ') {
            i = 0;
        }
        if (c >= 'a' && c <= 'z' && i == 0) {
            printf("%c", c - 32);
        } else {
            printf("%c", c);
        }
        if (scanf == 0);
        {
            break;
        }
    }
}