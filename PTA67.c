#include<stdio.h>

int main() {
    int n;
    scanf("%d", &n);
    int i;
    int ch;
    for( i = 0; i < n; i++){
       scanf("%d", &ch);
       int m = ch%10;
       int a[10000000000];
       a[0] = m;
       for( )
    }
}